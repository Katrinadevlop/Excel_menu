# 🏗️ Сборка приложения "Работа с меню" в exe файл

## 📋 Требования

- **Python 3.8+** (рекомендуется Python 3.9 или выше)
- Все зависимости из `requirements.txt`

## 🚀 Быстрый старт

### Способ 1: Автоматическая сборка (рекомендуется)

1. **Запустите батник:**
   ```
   Дважды кликните на build.bat
   ```

2. **Дождитесь завершения сборки**
   - Скрипт автоматически установит PyInstaller (если не установлен)
   - Создаст иконку приложения
   - Соберёт exe файл

3. **Найдите готовый exe файл:**
   ```
   dist/MenuApp.exe
   ```

### Способ 2: Ручная сборка

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите скрипт сборки:**
   ```bash
   python build_exe.py
   ```

3. **Или используйте PyInstaller напрямую:**
   ```bash
   python -m PyInstaller MenuApp.spec
   ```

## 📁 Структура проекта после сборки

```
excel_menu_gui/
├── main.py                    # Главный файл приложения
├── build_exe.py              # Скрипт сборки
├── build.bat                 # Батник для Windows
├── MenuApp.spec              # Конфигурация PyInstaller
├── app_icon.ico              # Иконка приложения
├── templates/                # Папка с шаблонами
├── build/                    # Временные файлы сборки
└── dist/
    └── MenuApp.exe           # 🎉 ГОТОВЫЙ EXE ФАЙЛ
```

## ⚙️ Параметры сборки

- **`--onefile`** - Один exe файл (не нужно носить DLL)
- **`--windowed`** - Без консольного окна (только GUI)
- **`--add-data`** - Включает папку templates в exe
- **`--hidden-import`** - Явно включает необходимые модули
- **`--icon`** - Добавляет иконку приложению

## 🔧 Возможные проблемы

### Python не найден
```bash
# Проверьте установку Python:
python --version

# Или попробуйте:
py --version
```

### PyInstaller не устанавливается
```bash
# Обновите pip:
python -m pip install --upgrade pip

# Установите PyInstaller:
pip install pyinstaller
```

### Ошибки при сборке
- Убедитесь, что все файлы (main.py, comparator.py, etc.) находятся в папке
- Проверьте, что папка `templates` существует
- Попробуйте удалить папки `build` и `dist`, затем пересобрать

### Большой размер exe файла
- Это нормально для первой сборки (~50-100 MB)
- Включены все библиотеки Qt и Python

## 📦 Результат

Готовый `MenuApp.exe` можно запускать на любом Windows компьютере без установки Python или дополнительных библиотек!

## 🎯 Функциональность exe

✅ Сравнение меню с подсветкой совпадений  
✅ Создание презентаций с данными  
✅ Скачивание шаблонов  
✅ Брокеражный журнал  
✅ Выбор места сохранения файлов  
✅ Автоматическое определение дат  
✅ Тёмная/светлая тема
